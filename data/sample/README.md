This folder contains small (300-row) samples of each generated table for quick inspection.
Run `python main.py` from the project root to regenerate the FULL 12-month dataset into data/raw/ and data/processed/ (full-scale files are excluded from this package to keep the download small).
